xx Add a check if all the previous scraped chap names and index are the same
    - If not add a Sentry Log
xx Add an expiry time to all celery tasks so no raceconditions happen
xx Create Token auth for backend
xx Change from crontab to schedule for celerybeat
*** Integrate Wellsaid into application to create real sounding voiceovers

from wuxiaworld.novels.models import Tag

tags = Tag.objects.all()
for tag in tags:
    if tags.filter(name = tag.name).count() > 1:
        print(tag)
    else:
        print("no",tag)